<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.examsys.oes.mapper.JudgeQuestionMapper">

    <select id="findByIdAndType" resultType="com.examsys.oes.entity.JudgeQuestion">
        SELECT * FROM "judge_question"
        WHERE questionId IN (SELECT questionId FROM "paper_manage" WHERE questionType = 3 AND paperId = #{paperId})
    </select>

    <select id="findAll" resultType="com.examsys.oes.entity.JudgeQuestion">
        SELECT * FROM "judge_question"
    </select>

    <select id="findOnlyQuestionId" resultType="com.examsys.oes.entity.JudgeQuestion">
        SELECT * FROM "judge_question" WHERE questionId = #{questionIds}
    </select>

    <insert id="add">
        INSERT INTO "judge_question" (subject, question, answer, analysis, level, section)
        VALUES (#{subject}, #{question}, #{answer}, #{analysis}, #{level}, #{section})
    </insert>

    <select id="findBySubject" resultType="java.lang.Integer">
        SELECT questionId FROM "judge_question"
        WHERE subject = #{subject} ORDER BY rand() DESC LIMIT #{pageNo}
    </select>

    <update id="edit">
        UPDATE "judge_question"
        SET subject = #{subject}, question = #{question}, answer = #{answer}, section = #{section}, analysis = #{analysis}, level = #{level}
        WHERE questionId = #{questionId}
    </update>

    <select id="findQuestionBySubject" resultType="com.examsys.oes.entity.JudgeQuestion">
        SELECT * FROM "judge_question" WHERE subject = #{subject}
    </select>

</mapper>
