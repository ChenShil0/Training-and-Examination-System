<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.examsys.oes.mapper.FillQuestionMapper">

    <select id="findByIdAndType" resultType="com.examsys.oes.entity.FillQuestion">
        SELECT * FROM "fill_question"
        WHERE questionId IN (SELECT questionId FROM "paper_manage" WHERE questionType = 2 AND paperId = #{paperId})
    </select>

    <select id="findAll" resultType="com.examsys.oes.entity.FillQuestion">
        SELECT * FROM "fill_question"
    </select>

    <select id="findOnlyQuestionId" resultType="com.examsys.oes.entity.FillQuestion">
        SELECT * FROM "fill_question" WHERE questionId = #{questionIds}
    </select>

    <insert id="add" useGeneratedKeys="true" keyProperty="questionId">
        INSERT INTO "fill_question" (subject, question, answer, analysis, level, section)
        VALUES (#{subject}, #{question}, #{answer}, #{analysis}, #{level}, #{section})
    </insert>

    <select id="findBySubject" resultType="java.lang.Integer">
        SELECT questionId FROM "fill_question"
        WHERE subject = #{subject} ORDER BY rand() DESC LIMIT #{pageNo}
    </select>

    <update id="edit">
        UPDATE "fill_question"
        SET section = #{section}, question = #{question}, answer = #{answer}, level = #{level}, analysis = #{analysis}
        WHERE questionId = #{questionId}
    </update>

    <select id="findQuestionBySubject" resultType="com.examsys.oes.entity.FillQuestion">
        SELECT * FROM "fill_question" WHERE subject = #{subject}
    </select>

</mapper>
