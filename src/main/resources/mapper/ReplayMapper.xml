<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.examsys.oes.mapper.ReplayMapper">

    <select id="findAll" resultType="com.examsys.oes.entity.Replay">
        SELECT messageId, replayId, replay, replayTime FROM "replay"
    </select>

    <select id="findAllById" resultType="com.examsys.oes.entity.Replay">
        SELECT messageId, replayId, replay, replayTime FROM "replay" WHERE messageId = #{messageId}
    </select>

    <select id="findById" resultType="com.examsys.oes.entity.Replay">
        SELECT messageId, replayId, replay, replayTime FROM "replay" WHERE messageId = #{messageId}
    </select>

    <delete id="delete">
        DELETE FROM "replay" WHERE replayId = #{replayId}
    </delete>

    <update id="update">
        UPDATE "replay"
        SET title = #{title}, replay = #{replay}, replayTime = #{replayTime}
        WHERE replayId = #{replayId}
    </update>

    <insert id="add" useGeneratedKeys="true" keyProperty="replayId">
        INSERT INTO "replay" (messageId, replay, replayTime)
        VALUES (#{messageId}, #{replay}, #{replayTime})
    </insert>

</mapper>
